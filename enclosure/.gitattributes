# Store text files with LF internally
* text=auto eol=lf

# ASCII STL is text, but should stay unchanged and free of diff noise
*.stl -text -diff

# Binary formats
*.png binary
*.jpg binary
*.3mf binary
